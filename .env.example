# FINDL Configuration Example

# Output directory for downloaded videos
OUTPUT_DIR=downloads

# Full path to your Widevine device file (.wvd)
# If not set, the tool looks for any .wvd in the root directory
WVD_PATH=./device.wvd

# Optional: Path to binaries if not in system PATH or default bin/ folder
# NM3U8DL_RE_PATH=path/to/N_m3u8DL-RE.exe
# SHAKA_PACKAGER_PATH=path/to/packager-win-x64.exe
